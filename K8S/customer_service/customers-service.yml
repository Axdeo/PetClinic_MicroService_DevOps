apiVersion: v1
kind: Service
metadata:
  labels:
    app: customers-service
  name: customers-service
  namespace: spring-petclinic
spec:
  type: ClusterIP
  selector:
    app: customers-service
  ports:
  - port: 8080
    targetPort: 8080
