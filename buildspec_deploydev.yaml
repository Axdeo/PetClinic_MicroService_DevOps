version: 0.2

phases:
  install:
    commands:
      - cd ${CODEBUILD_SRC_DIR}/K8S/
      - echo "Install phase"
      - bash ./install_dependencies.sh 

  build:
    commands:
      - echo "Deploy des charts"
      - bash ./deploy_charts.sh
artifacts:
  files:
    - '**/'